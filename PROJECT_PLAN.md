# 個人資料管理系統：專案計畫

## 1. 專案目標

建立一個適合作為金融業 IT／企業內部系統開發職缺求職作品的全端網站，展示下列能力：

- 使用者註冊、登入與登出。
- 登入後管理自己建立的資料，包含新增、查看、修改與刪除。
- 使用 PostgreSQL 持久化資料，重新整理或重新登入後資料仍然存在。
- 在伺服器端落實資料擁有權檢查，使用者只能存取自己的資料。
- 提供基本欄位驗證、清楚的錯誤提示與必要的操作紀錄。
- 完成測試、專案文件、測試帳號及公開部署。

本專案採教學式、小階段開發。每一階段都必須先說明目的，完成後記錄修改檔案、主要程式碼、啟動方式與測試結果，再進入下一階段。

> 資料安全原則：本作品只使用虛構測試資料，不輸入或保存真實身分證字號、金融帳號、密碼或其他敏感個資。

## 2. 第一版功能範圍

1. 使用者以 Email 與密碼註冊帳號。
2. 使用者登入及登出。
3. 登入後查看自己建立的資料列表。
4. 新增一筆個人資料紀錄。
5. 查看單筆資料紀錄。
6. 修改自己的資料紀錄。
7. 刪除自己的資料紀錄，操作前顯示確認提示。
8. 在前端提供易懂的欄位提示，並在伺服器端執行必要驗證。
9. 顯示操作成功、驗證失敗、查無資料及無權存取等訊息。
10. 新增、修改與刪除資料時建立操作紀錄。
11. 使用者無法查看或操作其他使用者的資料。
12. 提供 README、系統說明、測試方式與測試帳號。
13. 部署到可公開瀏覽的網址。

## 3. 第一版不做的功能

- 管理員後台或多角色權限。
- 忘記密碼、Email 驗證與雙因素驗證。
- 真實金融交易或真實敏感個資處理。
- 身分證字號、銀行帳號等高度敏感欄位。
- 檔案或大頭照上傳。
- 大量資料匯入、匯出及複雜搜尋。
- AI Agent、圖表、動畫、通知或其他非必要功能。
- 微服務、事件匯流排、快取層等過度設計。

## 4. 技術選擇

| 類別 | 選擇 | 理由 |
| --- | --- | --- |
| 全端框架 | Next.js（App Router） | 前後端可在同一專案開發，官方工具完整，適合作品集與 Vercel 部署。 |
| 程式語言 | TypeScript | 提供型別檢查，降低欄位與資料流錯誤，符合常見企業開發方式。 |
| 程式碼檢查 | ESLint | 在開發階段提早發現常見程式碼問題。 |
| 樣式 | Tailwind CSS | 可快速建立簡潔一致的介面，不需額外 UI 元件庫。 |
| 資料庫 | PostgreSQL | 主流關聯式資料庫，適合使用者、資料擁有權與稽核紀錄。 |
| ORM | Prisma ORM 穩定版 | Schema、Migration 與型別安全查詢較容易學習；不採用 Early Access 版本。 |
| 身分驗證 | Auth.js（預計） | 適合 Next.js；實作前會重新核對穩定版文件與帳號密碼流程。 |
| 欄位驗證 | Zod（預計） | 可用明確 Schema 做伺服器端驗證；只在驗證階段才安裝。 |
| 套件管理 | npm | Node.js 隨附、文件普遍，適合初學者。 |
| 部署 | Vercel（預計） | 與 Next.js 整合直接，適合建立公開作品網址。 |
| 雲端資料庫 | Neon PostgreSQL 或 Prisma Postgres（待評估） | 部署階段依官方穩定性、免費方案與 Vercel 整合情況再決定。 |

### `src` 資料夾決策

預計使用 `src` 資料夾，讓應用程式原始碼與根目錄設定檔、專案文件分離。這是 Next.js 官方建立工具支援的標準選項，不會額外增加架構複雜度。實際建立專案前仍需確認當時官方 CLI 選項。

## 5. 頁面規劃

| 預計路徑 | 用途 | 存取限制 |
| --- | --- | --- |
| `/` | 系統名稱、用途說明及登入／註冊入口 | 公開 |
| `/register` | 註冊帳號 | 未登入使用者 |
| `/login` | 登入 | 未登入使用者 |
| `/records` | 自己的資料列表 | 需登入 |
| `/records/new` | 新增資料 | 需登入 |
| `/records/[id]` | 查看單筆資料 | 僅資料擁有者 |
| `/records/[id]/edit` | 修改資料 | 僅資料擁有者 |
| `/activity` | 查看自己的操作紀錄 | 需登入 |

第 1 階段只建立 `/` 首頁。登入與註冊只顯示無功能的預留按鈕或連結，不建立對應頁面或驗證流程。

## 6. 資料表草圖

以下只記錄概念設計；第 0～1 階段不建立資料庫、Prisma Schema 或資料表。

### `User`

| 欄位 | 用途 |
| --- | --- |
| `id` | 使用者唯一識別碼 |
| `email` | 登入帳號，必須唯一 |
| `name` | 顯示名稱 |
| `passwordHash` | 雜湊後的密碼，不保存明文密碼 |
| `createdAt` | 建立時間 |
| `updatedAt` | 更新時間 |

### `PersonalRecord`

第一版欄位刻意保持簡單，不模擬真實個資核心系統，也不保存高度敏感資料。

| 欄位 | 用途 |
| --- | --- |
| `id` | 紀錄唯一識別碼 |
| `ownerId` | 紀錄擁有者，對應 `User.id` |
| `displayName` | 虛構資料的名稱或標題 |
| `email` | 虛構聯絡 Email |
| `phone` | 虛構聯絡電話，可選 |
| `note` | 簡短備註，可選 |
| `createdAt` | 建立時間 |
| `updatedAt` | 更新時間 |

所有讀取、修改與刪除操作都必須在伺服器端同時使用紀錄 ID 與目前登入者 ID 查詢，不能只靠前端隱藏按鈕。

### `AuditLog`

`AuditLog` 目前只保留在計畫中，不在第 0～1 階段建立資料表。

| 欄位 | 用途 |
| --- | --- |
| `id` | 操作紀錄唯一識別碼 |
| `userId` | 執行操作的使用者 |
| `recordId` | 相關資料紀錄，可選 |
| `action` | `CREATE`、`UPDATE` 或 `DELETE` |
| `details` | 必要摘要，不保存密碼或完整敏感資料 |
| `createdAt` | 操作時間 |

Auth.js 若採資料庫 Session，可能需要官方 Adapter 對應的其他資料表。實際資料表會在身分驗證階段依穩定版官方文件確認，不提前加入。

## 7. 開發階段與完成條件

### 第 0 階段：專案規劃

工作內容：

- 檢查工作區與 Node.js／npm 環境。
- 定義功能邊界、技術選擇、頁面、資料表草圖與里程碑。
- 建立並維護本文件。

完成條件：

- 工作區覆蓋風險已檢查。
- 第一版做與不做的範圍明確。
- 技術選擇、頁面及資料表草圖已記錄。
- 後續每一階段均有清楚完成條件。

### 第 1 階段：建立專案骨架

工作內容：使用 Next.js 官方建立工具產生 TypeScript、ESLint、Tailwind CSS、App Router 與 `src` 資料夾的 npm 專案，建立簡單首頁與 metadata。

完成條件：

- `npm run dev` 可啟動開發伺服器。
- `http://localhost:3000` 可顯示簡單首頁。
- 首頁只有系統名稱、用途、登入／註冊預留入口與骨架階段說明。
- lint 或 build 檢查通過。
- 未安裝 Prisma、Auth.js、Zod，未建立資料庫、API、驗證或 CRUD。

### 第 2 階段：資料庫與 Prisma 基礎

完成條件：

- 安裝 Prisma 穩定版並建立 PostgreSQL 開發資料庫連線。
- 建立經確認的初始 Schema 與 Migration。
- 能以開發工具確認資料表存在。
- 環境變數與機密資料未提交版本控制。

### 第 3 階段：註冊、登入與登出

完成條件：

- 密碼以可靠演算法雜湊後保存。
- 可完成註冊、登入與登出。
- 受保護頁面要求有效登入狀態。
- 已測試錯誤帳密、重複 Email 與未登入存取。

### 第 4 階段：新增與列表

完成條件：

- 登入者可新增經驗證的虛構資料紀錄。
- 列表只顯示目前登入者的資料。
- 重新整理與重新登入後資料仍存在。

### 第 5 階段：查看、修改與刪除

完成條件：

- 可查看、修改及刪除自己的紀錄。
- 刪除前需要確認。
- 成功與失敗狀態都有清楚提示。

### 第 6 階段：資料隔離與操作紀錄

完成條件：

- 所有資料操作都在伺服器端檢查 `ownerId`。
- 竄改網址或紀錄 ID 不能存取其他使用者資料。
- 新增、修改、刪除會建立不含敏感內容的 `AuditLog`。

### 第 7 階段：驗證、錯誤處理與測試

完成條件：

- 主要欄位具有前後端適當驗證。
- 常見錯誤有一致且不洩漏系統細節的訊息。
- 主要正常、驗證失敗及越權情境均有可重複測試步驟。

### 第 8 階段：文件與部署

完成條件：

- README 包含系統說明、安裝、啟動及測試方式。
- 建立只含虛構資料的測試帳號。
- 部署到公開網址並完成正式環境測試。
- 文件不包含資料庫密碼、Session Secret 或其他機密。

## 8. 目前進度

更新日期：2026-07-16（Asia/Taipei）

| 階段 | 狀態 | 說明 |
| --- | --- | --- |
| 第 0 階段 | 已完成 | 工作區為空白，已完成規劃並建立本文件。 |
| 第 1 階段 | 已完成 | 已在 `D:\Projects\動態網站\personal-data-management` 使用 Next.js 官方建立工具完成 TypeScript、ESLint、Tailwind CSS、App Router、`src` 目錄與 npm 專案骨架，並建立首頁及 metadata。 |
| 第 2～8 階段 | 尚未開始 | 不提前執行。 |

### 環境檢查紀錄

- 工作區：`D:\Projects\動態網站`
- 專案路徑：`D:\Projects\動態網站\personal-data-management`
- Node.js：`v24.18.0`
- npm：`11.16.0`；PowerShell 執行原則會攔截 `npm.ps1`，因此使用 `npm.cmd` 與 `npx.cmd`，未修改系統安全設定。
- Git：實際檢查版本為 `2.53.0.windows.3`。
- Visual Studio Code：`1.129.0`
- Next.js：`16.2.10`
- React／React DOM：`19.2.4`
- TypeScript：`^5`
- ESLint：`^9`
- Tailwind CSS：`^4`
- 工作區已由 OneDrive 搬移至本機路徑 `D:\Projects\動態網站`，後續專案建立於此路徑下。

### 第 1 階段測試結果

- `npm.cmd run lint`：通過，無 ESLint 錯誤。
- `npm.cmd run build`：通過；TypeScript 檢查、靜態頁面產生與 production build 均成功。
- `npm.cmd run dev`：開發伺服器成功啟動。
- `http://localhost:3000`：回傳 HTTP 200，已確認首頁包含「個人資料管理系統」、用途說明及第 1 階段提示。
- 未安裝 Prisma、Auth.js 或 Zod；未建立資料庫、API、登入、CRUD 或部署設定。
- npm 安裝稽核回報 2 個 moderate severity 相依套件弱點；第 1 階段未執行可能造成破壞性版本變更的 `npm audit fix --force`。

## 9. 重要技術決策紀錄

| 日期 | 決策 | 原因 |
| --- | --- | --- |
| 2026-07-16 | 採用 Next.js App Router、TypeScript、ESLint 與 Tailwind CSS | 主流、官方建立工具直接支援，適合逐步教學與全端作品。 |
| 2026-07-16 | 預計使用 `src` 資料夾 | 將應用程式碼與根目錄設定、文件分離，保持簡單且清楚。 |
| 2026-07-16 | 套件管理採 npm | 與 Node.js 一起提供，初學者文件與範例最多。 |
| 2026-07-16 | 資料庫採 PostgreSQL、ORM 採 Prisma 穩定版 | 展示關聯式資料設計與型別安全查詢，不使用 Early Access 產品。 |
| 2026-07-16 | 身分驗證預計採 Auth.js | 適合 Next.js；到實作階段再核對官方穩定流程，不提前安裝。 |
| 2026-07-16 | `PersonalRecord` 只保留簡單、虛構聯絡資料 | 避免把作品做成真實個資核心系統，降低敏感資料與範圍風險。 |
| 2026-07-16 | `AuditLog` 只在後續階段實作 | 第 0～1 階段只建立計畫與骨架，避免提前進入資料庫功能。 |
| 2026-07-16 | 第 1 階段使用 Next.js 官方 `create-next-app` 建立 | 環境已可使用 Node.js 與 npm；依計畫採用 TypeScript、ESLint、Tailwind CSS、App Router、`src` 目錄與 npm。 |
| 2026-07-16 | PowerShell 套件指令使用 `npm.cmd`／`npx.cmd` | 避免修改電腦執行原則，仍可正常執行 npm 官方工具。 |
| 2026-07-16 | 首頁不載入遠端 Google Fonts | 使用系統字型，避免本機開發及 production build 依賴外部字型網路連線。 |

## 10. 測試與啟動指令

目前 PowerShell 環境使用 `.cmd` 入口，避免 `npm.ps1` 受到執行原則攔截。

```powershell
node --version
npm.cmd --version
npm.cmd run dev
npm.cmd run lint
npm.cmd run build
```

開發伺服器網址：`http://localhost:3000`
